

spring:
  datasource:
    url: jdbc:postgresql://postgres-comments:5432/comments
    username: admin
    password: password
  flyway:
    enabled: true
    locations:
      - db.migration
    baseline-on-migrate: true

server:
  port: 8080
  ssl:
    enabled: true
    key-store: /opt/keystore.p12
    key-store-password: changeit
    key-store-type: PKCS12
    key-alias: spring-boot-ssl

frontend: ${FRONTEND_URL}
